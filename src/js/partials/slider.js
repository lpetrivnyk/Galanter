$('.header-page').slick({
    infinite: true,
    // arrows: false,
    autoplay: true,
    slidesToShow: 1,
    slidesToScroll: 1
  });

  $('.best-product-slider').slick({
    infinite: true,
    arrows: false,
    dots: true,
    // autoplay: true,
    cssEase: 'ease-out',
    slidesToShow: 4,
    slidesToScroll: 4
  });

  $('.novelty-slider').slick({
    infinite: true,
    arrows: false,
    autoplay: true,
    dots: true,
    slidesToShow: 1,
    slidesToScroll: 1
  });